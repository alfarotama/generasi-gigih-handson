// call giphy
// call search func
// make all in one (make appjs dummy)
// make the text input & search button to handle user's event

const Home = () => {
	return <div></div>;
};

export default Home;
